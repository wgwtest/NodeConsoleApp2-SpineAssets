# B1 官方样本bundle验证链路人工验收清单

状态：`待验收`

时间：`2026-04-07 20:25:40 +0800`

## 1. 核对范围

请围绕以下对象核对：

1. `workspace/samples/official/`
2. `workspace/exports/b1_official_samples/`
3. `workspace/preview/b1_official_samples/`
4. `tools/fetch_official_samples.mjs`
5. `tools/build_sample_bundle.mjs`
6. `tools/validate_sample_bundle.mjs`
7. `tools/build_sample_preview.mjs`

## 2. 验收项

### 2.1 官方样本

- [ ] 仓库内已保留 `spineboy` 与 `raptor` 两份最小官方样本
- [ ] 样本目录包含 `json / atlas / png / license` 基本文件
- [ ] `npm run samples:fetch` 可用于重建样本目录

### 2.2 正式 bundle

- [ ] `workspace/exports/b1_official_samples/bundle_manifest.json` 存在
- [ ] bundle 中包含 `spineboy` 与 `raptor` 两个角色
- [ ] 每个角色目录都包含 `character_manifest.json`
- [ ] 每个角色目录都包含 `json / atlas / png` 正式导出文件

### 2.3 校验与预览

- [ ] `npm run samples:validate` 执行通过
- [ ] `workspace/preview/b1_official_samples/index.html` 可打开
- [ ] `workspace/preview/b1_official_samples/report.json` 存在
- [ ] `workspace/preview/b1_official_samples/preview.png` 存在且内容可辨认

### 2.4 边界核对

- [ ] 当前链路没有引入 `.spine` 工程文件依赖
- [ ] 当前预览页没有提前耦合主工程运行时
- [ ] 当前输出仍以 `bundle_manifest / character_manifest` 契约为中心

## 3. 结论

- [ ] 通过
- [ ] 驳回

备注：

```text
如驳回，请写明具体问题、文件路径和复核建议。
```
