# A1 项目底座与契约建立 自测报告

## 1. 自测范围

本次自测覆盖：

1. 独立素材工程仓库可用性
2. `tools/sync_game_catalog.mjs` 的脚本级测试
3. 基于真实主工程目录的同步冒烟
4. 配置文件相对路径解析语义

## 2. 自测环境

1. 素材工程目录：`/home/wgw/CodexProject/NodeConsoleApp2-SpineAssets`
2. 主工程目录：`/home/wgw/CodexProject/NodeConsoleApp2/NodeConsoleApp2`
3. Node 运行方式：`node --test` 与 `node tools/sync_game_catalog.mjs`

## 3. 执行记录

### 3.1 脚本单测

执行命令：

```bash
node --test tools/sync_game_catalog.test.mjs
```

结果：

1. `2/2` 用例通过
2. 覆盖内容：
   - 从伪造主工程目录读取 `player/enemy`
   - `--config` 模式下相对路径相对配置文件目录解析

### 3.2 全量测试入口

执行命令：

```bash
npm test
```

结果：

1. 测试通过
2. 当前仓库无额外失败项

### 3.3 真实主工程同步冒烟

执行命令：

```bash
cp config/source-project.example.json config/source-project.json
npm run sync:catalog
```

结果：

1. 控制台输出 `SYNC OK players=1 enemies=14`
2. 成功读取主工程：
   - `assets/data/config.json`
   - `assets/data/player.json`
   - `assets/data/enemies.json`
3. 成功生成：
   - `workspace/imports/game_catalog.json`

## 4. 发现与修正

本轮自测中发现并修正了一个真实问题：

1. `config/source-project.json` 中的相对路径原先按执行目录解析
2. 修正后统一按配置文件所在目录解析
3. 已增加自动化用例锁定该行为，避免后续回归

## 5. 当前结论

1. `A1` 代码与文档基线已经可独立运行
2. 主工程与素材工程之间的首轮导入边界已经被真实数据验证
3. 当前状态适合进入人工复核，随后可继续推进 `B1`
