# 2026-04-07 B1 官方样本bundle验证链路自测报告

## 1. 自测范围

本轮自测覆盖：

1. `B1 样本角色导入Rig与导出验证`
2. 官方样本下载与本地重建
3. `bundle_manifest / character_manifest` 生成
4. bundle 校验
5. 静态预览页与截图证据

本轮不覆盖：

1. `.spine` 工程文件管理
2. 浏览器内 `Spine` runtime 播放
3. 主工程 `C1` 导入适配

## 2. 自测环境

1. 仓库目录：`/home/wgw/CodexProject/NodeConsoleApp2-SpineAssets`
2. Node.js：`v22.22.1`
3. 官方来源：`EsotericSoftware/spine-runtimes@4.2`
4. 样本：
   - `spineboy`
   - `raptor`

## 3. 执行记录

### 3.1 自动化测试

执行命令：

```bash
node --test tools/b1_official_bundle.test.mjs
```

结果：

1. `fetchOfficialSamples` 测试通过
2. `buildSampleBundle` 测试通过
3. `validateSampleBundle` 测试通过
4. `buildSamplePreview` 测试通过
5. 额外回归了 `fetch` 失败时的 `curl/downloadBinary` 回退逻辑

### 3.2 官方样本下载与重建

执行命令：

```bash
npm run samples:fetch
```

结果：

1. 成功从官方来源下载 `spineboy` 与 `raptor` 样本
2. 产物写入：
   - `workspace/samples/official/spineboy/`
   - `workspace/samples/official/raptor/`
3. 当前保留文件包括：
   - `*-pro.json`
   - `*-pma.atlas`
   - `*-pma.png`
   - `license.txt`

### 3.3 样本 bundle 组装

执行命令：

```bash
npm run samples:build
```

结果：

1. 成功输出 `workspace/exports/b1_official_samples/bundle_manifest.json`
2. 成功输出：
   - `workspace/exports/b1_official_samples/characters/spineboy/character_manifest.json`
   - `workspace/exports/b1_official_samples/characters/raptor/character_manifest.json`
3. 正式 bundle 内的导出文件名已折叠为稳定 ID：
   - `spineboy.json / spineboy.atlas / spineboy.png`
   - `raptor.json / raptor.atlas / raptor.png`

### 3.4 bundle 校验

执行命令：

```bash
npm run samples:validate
```

结果：

1. 输出 `VALIDATE OK characters=2 bundle=b1_official_samples`
2. 已校验：
   - `bundle_manifest.json` 存在且结构完整
   - 两份 `character_manifest.json` 存在且结构完整
   - skeleton、atlas、texture 文件均存在
   - 角色目录名与 `presentationId` 一致

### 3.5 静态预览与截图证据

执行命令：

```bash
npm run samples:preview
```

结果：

1. 生成静态预览页：
   - `workspace/preview/b1_official_samples/index.html`
2. 生成预览摘要：
   - `workspace/preview/b1_official_samples/report.json`
3. 生成角色预览素材副本：
   - `workspace/preview/b1_official_samples/assets/spineboy.png`
   - `workspace/preview/b1_official_samples/assets/raptor.png`
4. 通过浏览器打开本地预览页并生成截图：
   - `workspace/preview/b1_official_samples/preview.png`

## 4. 关键结果

### 4.1 bundle_manifest

关键字段：

1. `bundleId`: `b1_official_samples`
2. `bundleVersion`: `0.1.0`
3. 角色清单：
   - `spineboy`
   - `raptor`

### 4.2 character_manifest

抽样结果：

1. `spineboy`
   - `defaultSkin`: `default`
   - `animations`: `11`
   - `slots`: `52`
2. `raptor`
   - `defaultSkin`: `default`
   - `animations`: `5`
   - `slots`: `36`

## 5. 已知边界

1. 当前使用的是官方导出样本，不代表主工程真实角色目录
2. 当前从 `.json` skeleton 中提取动画名与槽位名，没有解析 `.skel`
3. 预览页当前为静态证据页，不承担运行时播放职责

这些边界符合本轮 `B1` 设计，不阻断样本 bundle 验证闭环成立。

## 6. 自测结论

1. `B1` 官方样本下载、bundle 构建、校验、预览链条已打通
2. 当前仓库已具备“保留最小样本 + 脚本可重建 + 交付 bundle + 静态证据”能力
3. 建议当前状态更新为：`已自测，待人工验收`
