# WBS L2：D1 主工程导入接入与 bundle 交付规范

## 1. 目标

把素材工程正式导出物纳入主工程消费链，并固定导入失败时的回退策略与诊断输出。

## 2. 进入条件

1. `阶段D` 已开始
2. `C1` 的 request-driven 导出契约已冻结
3. `B1` 样本 bundle 仍可通过主工程 probe

## 3. 预期输出

1. 主工程导入适配层
2. 失败诊断与降级策略
3. 对应双仓库验收脚本
